[package]
name = "label-proto"
version = "0.1.0"
edition = "2021"
description = "Shared data types for the label-hub node and control plane"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
